node_modules
.next
.DS_Store
.env.local
.env*.local
.firebase-emulator-data/

# Firebase Admin SDK service account key — never commit real credentials
physioonclick.json

# Debug logs from firebase-tools CLI runs
firebase-debug.log
firestore-debug.log

# Third-party plugin runtime state (not project source)
.claude-flow/
.claude/.proven-config-version
.claude/proven-config.json
sdd/
.impeccable/

# Cloudflare / OpenNext build output
.open-next/
.wrangler/
cloudflare-env.d.ts

# TS incremental build artifact — should never have been tracked
tsconfig.tsbuildinfo
